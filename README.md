# Troubleloop

A React + TypeScript + Vite starter for visualizing a DoubleLoop-style outcome cascade on a React Flow canvas.

The initial page maps team work, insights, product bets, and business drivers into a single connected outcome graph so teams can see how their work contributes to business outcomes.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
